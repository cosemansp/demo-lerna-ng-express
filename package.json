{
  "name": "my-app",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "test": "jest --verbose",
    "bootstrap": "lerna bootstrap",
    "postinstall": "npm run bootstrap",
    "precommit": "npm test",
    "build": "lerna run build"
  },
  "devDependencies": {
    "jest": "^21.2.1",
    "lerna": "^2.4.0",
    "prettier": "^1.7.4"
  }
}
